version: '3.7'
services:
    postgres:
        image: postgres:18-alpine
        environment:
            POSTGRES_DB: clap
            POSTGRES_PASSWORD: example
        ports:
            - '5432:5432'
        volumes:
            - ./.postgres-data:/var/lib/postgresql
